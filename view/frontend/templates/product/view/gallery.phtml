<div class="gallery">
    <ul id="lightSlider">
        <?php
    $galleryImages = $block->getGalleryImages();

    foreach ($galleryImages as $galleryImage) {
        $largeImageUrl = $galleryImage->getLargeImageUrl();
        $imageUrl = $galleryImage->getImageUrl();
        $smallImageUrl = $galleryImage->getSmallImageUrl(); ?>
        <li data-thumb="<?php echo $smallImageUrl; ?>">
            <a href="<?php echo $largeImageUrl; ?>"
                data-fancybox="group">
                <img src="<?php echo $imageUrl; ?>" />
            </a>
        </li>
        <?php
    }
    ?>
    </ul>
</div>
<script>
    require(['jquery', 'lightslider', 'fancybox', 'domReady!'], function($, lightSlider, fancyBox) {
        $('#lightSlider').lightSlider({
            gallery: true,
            item: 1,
            loop: true,
            slideMargin: 0,
            thumbItem: 6
        });
    });
</script>