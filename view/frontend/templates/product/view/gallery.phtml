<div class="gallery">
    <ul id="lightSlider">
        <?php
    $galleryImages = $block->getGalleryImages();

    foreach ($galleryImages as $galleryImage) {
        $largeImageUrl = $galleryImage->getLargeImageUrl();
        $imageUrl = $galleryImage->getMediumImageUrl();
        $smallImageUrl = $galleryImage->getSmallImageUrl(); ?>
        <li data-thumb="<?php echo $smallImageUrl; ?>">
            <a href="<?php echo $largeImageUrl; ?>"
                data-fancybox="group">
                <img src="<?php echo $imageUrl; ?>" width="280"
                    height="370" />
            </a>
        </li>
        <?php
    }
    ?>
    </ul>
</div>
<script>
    require(['jquery', 'lightslider', 'fancybox', 'domReady!'], function($, lightSlider) {
        $('#lightSlider').lightSlider({
            gallery: true,
            item: 1,
            loop: true,
            slideMargin: 0,
            thumbItem: 6
        });
    });
</script>