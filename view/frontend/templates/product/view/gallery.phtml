<div class="gallery">
    <ul id="lightSlider" style="padding-left: 25px;">
        <?php
    $galleryImages = $block->getGalleryImages();

    foreach ($galleryImages as $galleryImage) {
        $largeImageUrl = $galleryImage->getLargeImageUrl();
        $imageUrl = $galleryImage->getMediumImageUrl();
        $smallImageUrl = $galleryImage->getSmallImageUrl(); ?>
        <li data-thumb="<?php echo $smallImageUrl; ?>">
            <a href="<?php echo $largeImageUrl; ?>"
                data-fancybox="group">
                <img <?php
                // data-src
                // as https://stackoverflow.com/questions/57674943/how-do-i-lazy-load-images-in-jquery-lightslider
                ?>
                src="<?php echo $imageUrl; ?>" width="370"
                />
            </a>
        </li>
        <?php
    }
    ?>
    </ul>
</div>
<script>
    require(
        [
            'jquery',
            'lightSlider',
            'lightGallery',
            'lightGalleryVideo',
            'domReady!'
        ],
        function($, lightSlider,
            lightGallery) {
            $('#lightSlider').lightSlider({
                gallery: true,
                item: 1,
                loop: true,
                slideMargin: 0,
                thumbItem: 6
            });
            $('#lightSlider').lightGallery({});
        });
</script>